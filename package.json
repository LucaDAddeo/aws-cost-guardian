{
  "name": "aws-cost-guardian",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic web application for discovering, monitoring, and managing AWS resources that generate costs",
  "workspaces": [
    "src/lambdas",
    "src/frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "deploy:infra": "cd infrastructure && npx cdk deploy --all",
    "deploy:frontend": "cd src/frontend && npm run build && aws s3 sync dist/ s3://your-frontend-bucket"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
